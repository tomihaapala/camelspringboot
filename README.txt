This application listens HTTP POST -method's, json-type messages coming from webpage http://localhost:8080/camel/harkka/digia using REST.

Application parses json messages into Train and Location objects.

In your http://localhost:8080/ you will be able to click the individual train information links to visit google.maps and see the  
point of interest in the map. 